{
  "name": "pubilo-og",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "command -v cargo >/dev/null 2>&1 || { echo 'Rust toolchain is required: install cargo before building apps/og.'; exit 1; }; cargo install -q worker-build && worker-build --release",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  },
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
