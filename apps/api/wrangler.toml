#:schema node_modules/wrangler/config-schema.json
name = "pubilo-api-prod"
main = "src/index.ts"
compatibility_date = "2025-01-29"
compatibility_flags = ["nodejs_compat"]

[vars]
NODE_ENV = "production"
APP_ORIGIN = "https://pubilo.com"
API_ORIGIN = "https://api.pubilo.com"
FACEBOOK_APP_ID = "1451408483674705"
FACEBOOK_GRAPH_VERSION = "v21.0"

[[d1_databases]]
binding = "DB"
database_name = "pubilo-database-prod"
database_id = "59e18881-ca09-41a4-a980-8049c6c0dfb9"

[[r2_buckets]]
binding = "IMAGES"
bucket_name = "pubilo-media-prod"

# Cron Triggers (UTC timezone)
# * * * * * = Every minute - Auto-post
# 0 * * * * = Every hour - Health check (alert if no posts in last hour)
# 17:00 UTC = 00:00 UTC+7 (Thailand) - Fetch earnings from Facebook
# 09:00 UTC = 16:00 UTC+7 (Thailand) - Send LINE notification
[triggers]
crons = ["* * * * *", "0 * * * *", "0 17 * * *", "0 9 * * *"]

# Rules for importing binary files
[[rules]]
type = "Data"
globs = ["**/*.ttf", "**/*.wasm"]
fallthrough = true
